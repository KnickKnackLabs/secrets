#!/usr/bin/env bash
#MISE description="Store a secret in 1Password"
#USAGE arg "<key>" help="Secret key (e.g., baby-joel/github-pat)"
#USAGE flag "-v --value <value>" help="Value to store (or pipe via stdin)"

set -eo pipefail

KEY="${usage_key:-${1:?Usage: secrets 1password:set <key>}}"
VALUE="${usage_value:-}"
LIB_DIR="${MISE_CONFIG_ROOT:?}/lib"

source "$LIB_DIR/1password.sh"

if [ -n "$VALUE" ]; then
  op_set "$KEY" "$VALUE"
else
  op_set "$KEY"
fi
